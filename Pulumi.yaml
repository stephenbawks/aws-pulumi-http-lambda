name: pineapple-pizza
runtime:
  name: python
  options:
    virtualenv: venv
description: Creates a HTTP API Gateway with an integration to Eventbridge that will trigger rule(s).  The rule will target a SQS queue that in turn triggers a Lambda function.
